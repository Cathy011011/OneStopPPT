interface:
  display_name: "访谈分析报告 PPT 智能体"
  short_description: "先归纳访谈，再按本地参考模板生成和精修可编辑 PPT"
  default_prompt: "Use $interview-summary-workflow first when the user provides raw interview notes, then use $avatr-report-ppt to map the approved summary into the local reference PPT structure, keep the deck editable, and run rendered QA for font size, overlap, and layout balance."
